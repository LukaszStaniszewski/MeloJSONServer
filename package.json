{
  "scripts": {
    "serve": "json-server db.json -m ./node_modules/json-server-auth --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.1",
    "json-server-auth": "^2.1.0"
  }
}
